	for fqdn := req.Question[0].Name; fqdn != ""; {
		ip, ok := s.domainEDNSAddr[fqdn]
		if ok {
			prx.EnableEDNSClientSubnet = true
			prx.EDNSAddr = ip
			break
		}
		ip, ok = s.domainEDNSAddr["*."+fqdn]
		if ok {
			prx.EnableEDNSClientSubnet = true
			prx.EDNSAddr = ip
			break
		}

		_, fqdn, _ = strings.Cut(fqdn, ".")
	}

	if dctx.err = prx.Resolve(pctx); dctx.err != nil {
